import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Card, NavigationOption, Section } from '@transferwise/components';
import { FastFlag as FastFlagIcon } from '@transferwise/icons';
import code from '../../../liveEditorCode/section.code';

<LiveEditorBlock code={code} scope={{ Card, FastFlagIcon, NavigationOption, Section }} />
<GeneratePropsTable componentName="Section" />

export const meta = {
  name: 'Section',
  badge: {
    type: 'new',
    expiryDate: '2021-10-24',
  },
};
